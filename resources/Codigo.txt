prog
BEGIN
	integer adan, boro, andrea;
	boro := -32768;
	adan := 32768;
	adan boro;
	integer FUN juancito ( double a ) BEGIN 
	    WHILE ((-32768)>(-5.8))BEGIN OUTF(1); OUTF(0) END;
	    a:=funcion(b);
	    IF((a,3+4)=(b,35))THEN A:=1;
	    ELSE B:=0;END_IF;
	    RET (a);
	    a:=funcion(b);
	 END;
	 TYPEDEF TRIPLE < integer > a;
	 RET (0); 
END
