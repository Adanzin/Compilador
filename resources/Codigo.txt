prog
BEGIN
	integer adan, boro, andrea;
	boro := -32768;
	adan := 32768;
	adan boro;
	integer FUN juancito ( double a ) BEGIN 
	    WHILE ((-32768)>(-5.8)) OUTF(integer a);
	    IF((a,3+4)=(b,35))THEN 
	        x:=2;
	        ELSE a:=5; 
	    END_IF;
	    RET (a);
	    a:=funcion(b);
	 END;
	 TYPEDEF TRIPLE < integer > a;
	 RET (0); 
END